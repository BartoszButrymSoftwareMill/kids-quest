# E2E Test Environment Variables
# Copy this file to .env.test and fill in your values

# Supabase Configuration (for both app and test client)
PUBLIC_SUPABASE_URL=your_supabase_url_here
PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Supabase Service Role Key (REQUIRED for E2E tests with email confirmation)
# Used to automatically confirm user emails in tests
# Find in: Supabase Dashboard → Project Settings → API → service_role (secret)
# ⚠️ WARNING: Never commit this key or use it in client-side code!
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

# E2E Test User Credentials (OPTIONAL - not required anymore)
# Leave empty - tests will create users dynamically
E2E_USERNAME=
E2E_PASSWORD=
E2E_USERNAME_ID=

# OpenRouter API Key (for AI generation tests)
# Leave empty to skip AI generation tests
OPENROUTER_API_KEY=
